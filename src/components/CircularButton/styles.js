import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  text: {
    color: "white",
    // fontSize: 17,
    // marginTop: "25%",
    textAlign: "center",
    fontWeight: 'bold'
  }
});

export default styles;